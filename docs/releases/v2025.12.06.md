# sfetch v2025.12.06

## Checklist
- [x] `go test ./...`
- [x] `make precommit` (includes shell-check)
- [x] Minisign verification tested
- [x] Install script tested with embedded trust anchor

## Changes

1. **Minisign verification**
   - `--minisign-key` flag for pure-Go `.minisig` parsing
   - No external dependencies for signature verification

2. **Bootstrap installer**
   - `install-sfetch.sh` with embedded minisign public key (trust anchor)
   - Platform detection (linux/darwin/windows, amd64/arm64)
   - Options: `--dir`, `--tag`, `--dry-run`, `--yes`
   - Included in SHA256SUMS for pre-pipe verification

3. **Dual signing**
   - SHA256SUMS signed with minisign (.minisig) and PGP (.asc)
   - Users choose preferred verification method

4. **CI/CD**
   - `shell-check` target added to precommit (shellcheck + shfmt)
   - Release workflow uploads install-sfetch.sh
   - CI installs shellcheck and shfmt

5. **Documentation**
   - Pre-pipe verification examples (minisign + GPG)
   - Key rotation checklist in signing runbook
   - curl and wget examples for bootstrap

## Deployment Notes
- Tag: `v2025.12.06`
- Signing: `MINISIGN_KEY` and `PGP_KEY_ID` required for dual signing
- Trust anchor: `RWTAoUJ007VE3h8tbHlBCyk2+y0nn7kyA4QP34LTzdtk8M6A2sryQtZC`
