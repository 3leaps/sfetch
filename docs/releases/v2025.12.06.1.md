# sfetch v2025.12.06.1

Patch release fixing install script bug from v2025.12.06.

## Checklist
- [x] `make precommit` (includes shell-check)
- [x] Install script tested locally with `--dry-run`
- [x] CI updated to test install script

## Changes

1. **Bug fixes**
   - Fixed EXIT trap error in install-sfetch.sh (`tmpdir: unbound variable`)
   - Fixed SHA256SUMS containing full paths (`dist/release/...`) instead of basenames

2. **CI improvements**
   - Added install script dry-run test against v2025.12.05
   - Catches runtime issues that shellcheck can't detect

3. **Documentation**
   - README verification uses `shasum -a 256` (macOS compatible)
   - GPG verification uses temp keyring (`GPG_TMPDIR`) to avoid polluting user's keychain

4. **Release tooling**
   - Upload script now includes `SHA256SUMS.minisig` and `sfetch-minisign.pub`
   - Updated RELEASE_CHECKLIST.md with complete dual-signing workflow

## Deployment Notes
- Tag: `v2025.12.06.1`
- Supersedes: `v2025.12.06` (yanked due to install script bug)
- Trust anchor unchanged: `RWTAoUJ007VE3h8tbHlBCyk2+y0nn7kyA4QP34LTzdtk8M6A2sryQtZC`
