# sfetch v2025.12.05

## Checklist
- [x] `go test ./...`
- [x] `make precommit`
- [x] Integration test run with goneat (PGP auto-detect)

## Changes
1. **User experience**
   - Embedded quickstart documentation accessible via `sfetch -helpextended`.
   - README updated with build/install instructions and key lookup order.
2. **Security & verification**
   - `--pgp-key-url`, `--pgp-key-asset`, and auto-detection for `.asc` keys.
   - Local, URL, and release-asset keys tested via `TestResolvePGPKey`.
3. **Packaging**
   - `make install` installs into user-space (`~/.local/bin` or `%USERPROFILE%\bin`).
   - Release workflow renames binaries inside archives (`sfetch`/`sfetch.exe`).
   - Bootstrap script downloads the correct archive and installs `sfetch`.
4. **Docs**
   - Added `docs/pattern-matching.md` and `docs/quickstart.txt` for heuristic explanation and CLI help.
   - `CHANGELOG.md` and `RELEASE_NOTES.md` started for CalVer releases.

## Deployment Notes
- Tag this commit as `v2025.12.05`.
- Ensure `PGP_PRIVATE_KEY` / `PGP_KEY_PASSPHRASE` secrets exist before running the release workflow.
- After release, update Homebrew/Scoop recipes to point to the new archive names if needed.
